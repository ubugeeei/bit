// Generated by moon-component (fixed for MoonBit compatibility)
// Import module: bit:git/filesystem

///|
/// Low-level FFI import
fn ffi_is_dir(p0_ptr : Int, p0_len : Int) -> Int = "bit:git/filesystem" "is-dir"

///|
/// High-level wrapper with canonical ABI conversion
pub fn is_dir(path : String) -> Bool {
  let (p0_ptr, p0_len) = @cabi.cabi_lower_string(path)
  let result = ffi_is_dir(p0_ptr, p0_len)
  result != 0
}

///|
/// Low-level FFI import
fn ffi_is_file(p0_ptr : Int, p0_len : Int) -> Int = "bit:git/filesystem" "is-file"

///|
/// High-level wrapper with canonical ABI conversion
pub fn is_file(path : String) -> Bool {
  let (p0_ptr, p0_len) = @cabi.cabi_lower_string(path)
  let result = ffi_is_file(p0_ptr, p0_len)
  result != 0
}

///|
/// Low-level FFI import
fn ffi_mkdir_p(p0_ptr : Int, p0_len : Int, retptr : Int) -> Int = "bit:git/filesystem" "mkdir-p"

///|
/// High-level wrapper with canonical ABI conversion
pub fn mkdir_p(path : String) -> Result[Unit, String] {
  let (p0_ptr, p0_len) = @cabi.cabi_lower_string(path)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 12)
  let _ = ffi_mkdir_p(p0_ptr, p0_len, retptr)
  match @cabi.cabi_read_u8(retptr).to_int() {
    0 => Ok(())
    _ =>
      Err(
        @cabi.cabi_lift_string(
          @cabi.cabi_read_i32(retptr + 4),
          @cabi.cabi_read_i32(retptr + 4 + 4),
        ),
      )
  }
}

///|
/// Low-level FFI import
fn ffi_read_file(p0_ptr : Int, p0_len : Int, retptr : Int) -> Int = "bit:git/filesystem" "read-file"

///|
/// High-level wrapper with canonical ABI conversion
pub fn read_file(path : String) -> Result[Array[Byte], String] {
  let (p0_ptr, p0_len) = @cabi.cabi_lower_string(path)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 12)
  let _ = ffi_read_file(p0_ptr, p0_len, retptr)
  match @cabi.cabi_read_u8(retptr).to_int() {
    0 => {
      let lp0 = @cabi.cabi_read_i32(retptr + 4)
      let ll0 = @cabi.cabi_read_i32(retptr + 4 + 4)
      let la0 : Array[Byte] = Array::new(capacity=ll0)
      for li0 = 0; li0 < ll0; li0 = li0 + 1 {
        la0.push(@cabi.cabi_read_u8(lp0 + li0 * 1))
      }
      Ok(la0)
    }
    _ =>
      Err(
        @cabi.cabi_lift_string(
          @cabi.cabi_read_i32(retptr + 4),
          @cabi.cabi_read_i32(retptr + 4 + 4),
        ),
      )
  }
}

///|
/// Low-level FFI import
fn ffi_readdir(p0_ptr : Int, p0_len : Int, retptr : Int) -> Int = "bit:git/filesystem" "readdir"

///|
/// High-level wrapper with canonical ABI conversion
pub fn readdir(path : String) -> Result[Array[String], String] {
  let (p0_ptr, p0_len) = @cabi.cabi_lower_string(path)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 12)
  let _ = ffi_readdir(p0_ptr, p0_len, retptr)
  match @cabi.cabi_read_u8(retptr).to_int() {
    0 => {
      let lp0 = @cabi.cabi_read_i32(retptr + 4)
      let ll0 = @cabi.cabi_read_i32(retptr + 4 + 4)
      let la0 : Array[String] = Array::new(capacity=ll0)
      for li0 = 0; li0 < ll0; li0 = li0 + 1 {
        la0.push(
          @cabi.cabi_lift_string(
            @cabi.cabi_read_i32(lp0 + li0 * 8),
            @cabi.cabi_read_i32(lp0 + li0 * 8 + 4),
          ),
        )
      }
      Ok(la0)
    }
    _ =>
      Err(
        @cabi.cabi_lift_string(
          @cabi.cabi_read_i32(retptr + 4),
          @cabi.cabi_read_i32(retptr + 4 + 4),
        ),
      )
  }
}

///|
/// Low-level FFI import
fn ffi_remove_dir(p0_ptr : Int, p0_len : Int, retptr : Int) -> Int = "bit:git/filesystem" "remove-dir"

///|
/// High-level wrapper with canonical ABI conversion
pub fn remove_dir(path : String) -> Result[Unit, String] {
  let (p0_ptr, p0_len) = @cabi.cabi_lower_string(path)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 12)
  let _ = ffi_remove_dir(p0_ptr, p0_len, retptr)
  match @cabi.cabi_read_u8(retptr).to_int() {
    0 => Ok(())
    _ =>
      Err(
        @cabi.cabi_lift_string(
          @cabi.cabi_read_i32(retptr + 4),
          @cabi.cabi_read_i32(retptr + 4 + 4),
        ),
      )
  }
}

///|
/// Low-level FFI import
fn ffi_remove_file(p0_ptr : Int, p0_len : Int, retptr : Int) -> Int = "bit:git/filesystem" "remove-file"

///|
/// High-level wrapper with canonical ABI conversion
pub fn remove_file(path : String) -> Result[Unit, String] {
  let (p0_ptr, p0_len) = @cabi.cabi_lower_string(path)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 12)
  let _ = ffi_remove_file(p0_ptr, p0_len, retptr)
  match @cabi.cabi_read_u8(retptr).to_int() {
    0 => Ok(())
    _ =>
      Err(
        @cabi.cabi_lift_string(
          @cabi.cabi_read_i32(retptr + 4),
          @cabi.cabi_read_i32(retptr + 4 + 4),
        ),
      )
  }
}

///|
/// Low-level FFI import
fn ffi_write_file(
  p0_ptr : Int,
  p0_len : Int,
  p1_ptr : Int,
  p1_len : Int,
  retptr : Int,
) -> Int = "bit:git/filesystem" "write-file"

///|
/// High-level wrapper with canonical ABI conversion
pub fn write_file(path : String, content : Array[Byte]) -> Result[Unit, String] {
  let (p0_ptr, p0_len) = @cabi.cabi_lower_string(path)
  let p1_len = content.length()
  let p1_ptr = @cabi.cabi_realloc(0, 0, 1, p1_len * 1)
  for i, elem in content {
    let elem_ptr = p1_ptr + i * 1
    @cabi.cabi_write_u8(elem_ptr, elem)
  }
  let retptr = @cabi.cabi_realloc(0, 0, 4, 12)
  let _ = ffi_write_file(p0_ptr, p0_len, p1_ptr, p1_len, retptr)
  match @cabi.cabi_read_u8(retptr).to_int() {
    0 => Ok(())
    _ =>
      Err(
        @cabi.cabi_lift_string(
          @cabi.cabi_read_i32(retptr + 4),
          @cabi.cabi_read_i32(retptr + 4 + 4),
        ),
      )
  }
}

///|
/// Low-level FFI import
fn ffi_write_string(
  p0_ptr : Int,
  p0_len : Int,
  p1_ptr : Int,
  p1_len : Int,
  retptr : Int,
) -> Int = "bit:git/filesystem" "write-string"

///|
/// High-level wrapper with canonical ABI conversion
pub fn write_string(path : String, content : String) -> Result[Unit, String] {
  let (p0_ptr, p0_len) = @cabi.cabi_lower_string(path)
  let (p1_ptr, p1_len) = @cabi.cabi_lower_string(content)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 12)
  let _ = ffi_write_string(p0_ptr, p0_len, p1_ptr, p1_len, retptr)
  match @cabi.cabi_read_u8(retptr).to_int() {
    0 => Ok(())
    _ =>
      Err(
        @cabi.cabi_lift_string(
          @cabi.cabi_read_i32(retptr + 4),
          @cabi.cabi_read_i32(retptr + 4 + 4),
        ),
      )
  }
}
