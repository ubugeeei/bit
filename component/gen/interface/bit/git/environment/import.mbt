// Generated by moon-component
// Import module: bit:git/environment

///|
/// Low-level FFI import
fn ffi_get_env(p0_ptr : Int, p0_len : Int, retptr : Int) -> Int = "bit:git/environment" "get-env"

///|
/// High-level wrapper with canonical ABI conversion
pub fn get_env(name : String) -> String? {
  let (p0_ptr, p0_len) = @cabi.cabi_lower_string(name)
  let retptr = @cabi.cabi_realloc(0, 0, 4, 12)
  let _ = ffi_get_env(p0_ptr, p0_len, retptr)
  match @cabi.cabi_read_u8(retptr).to_int() {
    0 => None
    _ =>
      Some(
        @cabi.cabi_lift_string(
          @cabi.cabi_read_i32(retptr + 4),
          @cabi.cabi_read_i32(retptr + 4 + 4),
        ),
      )
  }
}

///|
pub fn now() -> Int64 = "bit:git/environment" "now"
